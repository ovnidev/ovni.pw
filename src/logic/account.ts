export const createAccount = (service: string, folder: string, password: string) => {
    
}

export const updateAccount = (id: string) => {
    
}

export const deleteAccount = (id: string) => {
    
}